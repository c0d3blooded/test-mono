import Image from 'next/image';
import { Button, Chip, ChipGroup, Input } from '@treelof/components';
import { SlideVertical } from '@treelof/animations';
import WikiCharacteristicsTable from '../../components/wiki/characteristics-table';

<div className="flex flex-1 flex-col md:space-y-4 space-y-2">
  {/* props
      plant: Plant item
      methods: react-hook-form methods 
      saving: the form is being saved
      error: (optional) string error
  */}
  <div className="pb-6">
    <Button
      color="secondary"
      buttonProps={{ type: 'submit' }}
      loading={props.saving}
    >
      Submit Revision
    </Button>
    {/* show error  */}
    <SlideVertical show={Boolean(props.error)}>
      <p className="mt-2 -mb-2 text-sm text-red-600">{props.error}</p>
    </SlideVertical>
  </div>
  <Input
    label="Common name"
    inputProps={{
      disabled: true,
      placeholder: "The plant's common name",
      ...props.methods.register('common_name', {
        required: 'Please enter a common name'
      })
    }}
  />
  <Input
    label="Botanical name"
    inputProps={{
      disabled: true,
      placeholder: "The plant's scientific name",
      ...props.methods.register('botanical_name', {
        required: "Please enter this plant's botnanical name"
      })
    }}
  />
</div>

---

#### Characteristics

<WikiCharacteristicsTable plant={props.plant} edit />

---

<Input
  label="Description"
  textAreaProps={{
    disabled: true,
    rows: 10,
    placeholder: "The plant's description",
    ...props.methods.register('description')
  }}
  multiline
/>

---

#### References

Under development
